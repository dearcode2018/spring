
文件上传
1) 上传单个文件，使用一个name
2) 上传多个文件，可以使用一个相同的name或多个不同的name
3) 使用不同的name，例如 file1 file2 file3 ... 构成多个key的多个对象
4) 使用相同的name，例如file，构成单个key的一个数组

文件上传name场景
1) 上传单个文件，name="file"
2) 上传多个文件，name="file"
3) 上传多个文件，name="file1" name="file2"
注意: 不能混合多个场景，否则部分文件将会上传失败.
















